import { Sizes, Colors, AlignType } from "../../typings";

export interface IconProps {
  width?: Sizes;
  height?: Sizes;
  fillPath?: string;
  align?: AlignType;
  fill?: Colors;
  stroke?: Colors;
}
